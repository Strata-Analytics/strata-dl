name: strata-dl-pipeline
description: Pipeline for datalake project
runtime:
  name: nodejs
  options:
    packagemanager: npm
    nodeargs: "--import=tsx"
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
